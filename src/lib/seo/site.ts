// Single source of truth for site identity used by metadata + JSON-LD.
export const BASE_URL = 'https://orcaclub.pro'
export const SITE_NAME = 'ORCACLUB'
export const ORG_ID = `${BASE_URL}/#organization`
export const WEBSITE_ID = `${BASE_URL}/#website`
// TODO(rework Phase D): asset does not exist yet — every OG card is broken until it ships.
export const DEFAULT_OG_IMAGE = '/og-image.jpg'
